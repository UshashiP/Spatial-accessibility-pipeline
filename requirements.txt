# ── Core geospatial ──────────────────────────────────────────────────────────
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0
fiona>=1.9.0
pyarrow>=15.0.0          # GeoParquet read/write

# ── Data science ─────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.4.0

# ── AWS ──────────────────────────────────────────────────────────────────────
boto3>=1.34.0
botocore>=1.34.0

# ── DuckDB ───────────────────────────────────────────────────────────────────
duckdb>=0.10.0

# ── HTTP / API clients ────────────────────────────────────────────────────────
requests>=2.31.0
census>=0.8.19           # census python wrapper (Census Bureau API)

# ── Config & utilities ────────────────────────────────────────────────────────
PyYAML>=6.0.1
python-dotenv>=1.0.0
tenacity>=8.2.0          # retry logic for API calls

# ── Orchestration (Airflow) ──────────────────────────────────────────────────
# apache-airflow>=2.9.0
# apache-airflow-providers-amazon>=8.20.0
# Note: Airflow is heavy; install separately for orchestration workflows

# ── Visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.8.0
pillow>=10.0.0
streamlit>=1.36.0
streamlit-folium>=0.15.0  # Folium integration for Streamlit
folium>=0.14.0            # interactive maps
s3fs>=2024.3.1
openpyxl>=3.1.0           # Excel export

# ── Dash (Plotly) Dashboard ──────────────────────────────────────────────────
# dash>=2.14.0              # High-performance interactive dashboards
# dash-bootstrap-components>=1.5.0  # Bootstrap UI components for Dash
plotly>=5.18.0            # Interactive plots and maps
# kaleido>=0.2.1            # Static image export for Plotly

# ── Testing & Code Quality ───────────────────────────────────────────────────
pytest>=8.1.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
ruff>=0.4.0             # Fast Python linter
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# ── Code quality ─────────────────────────────────────────────────────────────
ruff>=0.4.0

# ── Database (optional – kept for local PostGIS work) ────────────────────────
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
GeoAlchemy2>=0.14.0


geoalchemy2
psycopg2-binary
sqlalchemy
dbt-postgres
ruff

streamlit>=1.32.0
geopandas>=0.14.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
folium>=0.15.0
pyarrow>=14.0.0
shapely>=2.0.0
branca>=0.7.0
